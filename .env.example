# Dermato XAI — annotation flow
# https://services.clarin-pl.eu — LLM feedback for annotation comparison
CLARIN_API_KEY=****
# Optional; defaults to llama3.3. List available models with GET /api/v1/oapi/models.
# CLARIN_MODEL=llama3.3

# Modal RF-DETR inference endpoint
MODAL_RFDETR_URL=https://szafraniecszymon--dermoscopy-rfdetr-sahi-inference-infer-05a6e2.modal.run

# unset: all 5 structures; 1: Yellow globules + Blue-gray globules; 2: Yellow globules only
# ANNOTATION_STRUCTURE_SET=1
